View(selfesteem)
res.aov <- anova_test(data = selfesteem, dv = score, wid = id, within = time)
get_anova_table(res.aov)
selfesteem2 <- selfesteem2 %>%
gather(key = "time", value = "score", t1, t2, t3) %>%
convert_as_factor(id, time)
data("selfesteem2", package = "datarium")
selfesteem2 <- selfesteem2 %>%
gather(key = "time", value = "score", t1, t2, t3) %>%
convert_as_factor(id, time)
res.aov <- anova_test(
data = selfesteem2, dv = score, wid = id,
within = c(treatment, time)
)
get_anova_table(res.aov)
data <- read_csv(file="BasicRawSummary.csv")
data = data[-which(is.na(data$target_in_count)), ]
row.names(data) = NULL
data$id = 1:6874
data %>%
group_by(posture, cursor_type) %>%
get_summary_stats(initial_contact_time, type = "mean_sd")
res.aov <- anova_test(
data = data, dv = initial_contact_time,wid=id,
within = c(posture, cursor_type)
)
data <- read_csv(file="BasicRawSummary.csv")
data$posture = as.factor(data$posture)
data$cursor_type = as.factor(data$cursor_type)
data = data[-which(is.na(data$target_in_count)), ]
data$id = 1:6874
data <- data %>%
gather(key = "rep", value = "rep_count", repetition) %>%
convert_as_factor(id, time)
data %>%
group_by(posture, cursor_type) %>%
get_summary_stats(initial_contact_time, type = "mean_sd")
res.aov <- anova_test(
data = data, dv = initial_contact_time,wid=id,
within = c(posture, cursor_type)
)
data <- read.csv("BasicRawSummary.csv")
# data$posture = as.factor(data$posture)
# data$cursor_type = as.factor(data$cursor_type)
data = data[-which(is.na(data$target_in_count)), ]
row.names(data) = NULL
cols = colnames(data)[13:25]
out = data.frame()
for (var in cols) {
var_name = paste0("mean_", var)
data.ddply = ddply(data, .(subject_num, posture, cursor_type, repetition) , summarize, mean_var = mean(eval(parse(text = var))))
data.ddply <- data.ddply %>% mutate(id = paste0(subject_num,"_", repetition))
result <- data.ddply %>% anova_test(mean_var ~
posture * cursor_type +
Error(id/(posture*cursor_type)))
sphericities = result[["Mauchly's Test for Sphericity"]][["p"]]
pVals = c(paste0(result[["ANOVA"]][["DFn"]][1], ", ",result[["ANOVA"]][["DFd"]][1]), result[["ANOVA"]][["p"]][1])
for (i in 1:length(sphericities)) {
if (sphericities[i] < 0.05) {
pVals = c(pVals, result[["Sphericity Corrections"]][["DF[GG]"]][i], result[["Sphericity Corrections"]][["p[GG]"]][i], "TRUE")
} else {
pVals = c(pVals, paste0(result[["ANOVA"]][["DFn"]][i+1], ", " , result[["ANOVA"]][["DFd"]][i+1]), result[["ANOVA"]][["p"]][i+1], "FALSE")
}
}
out = rbind(out, c(var_name, pVals))
}
colnames(out) = c("var","df_posture", "p_posture","df_cursor",  "p_cursor", "sphericity_correction_cursor","df_posture*cursor", "p_posture*cursor", "sphericity_correction_posture*cursor")
View(out)
cols
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean')
View(data.mean)
data.mean <- aggregate(data,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean')
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
data = data[-which(is.na(data$target_in_count)), ]
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean')
data <- read_csv(file="BasicRawSummary.csv")
data$posture = as.factor(data$posture)
data$cursor_type = as.factor(data$cursor_type)
data = data[-which(is.na(data$target_in_count)), ]
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean')
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean',na.rm=TRUE)
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),anova_table=list(es='pes'))
library(afex)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),anova_table=list(es='pes'))
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),anova_table=list(es='pes'))
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean'
# ,na.rm=TRUE
)
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'))
View(data.mean)
colnames(data.mean) = c('subject_num','posture','cursor_type','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'))
summary(a1)
View(a1)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'), anova_table = list(es = "pes"))
summary(a1)
summary(a1)$sphericity.tests
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
m1 = emmeans(a1,~posture)
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
library(emmeans)
library(emmeans)
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
install.packages("rvlenth")
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
library(ggplot2)
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
library(ggbeeswarm)
afex_plot(a1, x = "posture",error="within")																		.
library(ggbeeswarm)
#plot(twoWayRM$nailDetail, twoWayRM$duration)
afex_plot(a1, x = "nail",error="within")
# apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
# apa_print(a1, correction = "GG", es="pes")$full_result
# m1 = emmeans(a1,~posture)
afex_plot(a1, x = "posture",error="within")
afex_plot(a1, x = "cursor_type",error="within")
m1=emmeans(a1,~posture)
summary(as.glht(pairs(m1)), test=adjusted("bonferroni"))
install.packages(multcomp)
install.packages("multcomp")
library(multcomp)
m1=emmeans(a1,~posture)
summary(as.glht(pairs(m1)), test=adjusted("bonferroni"))
View(m1)
m1=emmeans(a1,~ posture)
summary(as.glht(pairs(m1)), test=adjusted("bonferroni"))
m1=emmeans(a1,~ cursor_type)
summary(as.glht(pairs(m1)), test=adjusted("bonferroni"))
as.glht(pairs(m1)), test=adjusted("bonferroni")
as.glht(pairs(m1))
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type),
FUN = 'mean'
# ,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','initial_contact_time')
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'), anova_table = list(es = "pes"))
summary(a1)$sphericity.tests
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),
# anova_table = list(es = "pes")
)
summary(a1)$sphericity.tests
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),
# anova_table = list(es = "pes")
)
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
library("papaja")
install.packages(papaja)
install.packages('papaja')
if(!"tinytex" %in% rownames(installed.packages())) install.packages("tinytex")
tinytex::install_tinytex()
library("papaja")
# Install devtools package if necessary
if(!"devtools" %in% rownames(installed.packages())) install.packages("devtools")
# Install the stable development verions from GitHub
devtools::install_github("crsh/papaja")
# Install the latest development snapshot from GitHub
devtools::install_github("crsh/papaja@devel")
library("papaja")
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
a1
a1
apa_print.aov(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),
anova_table = list(es = "pes")
)
library("papaja")
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
# ,na.rm=TRUE
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type'),
anova_table = list(es = "pes")
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
# anova_table = list(es = "pes")
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
# ,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
View(data.mean)
View(data.mean)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes"),
fun_aggregate = 'mean'
)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes"),
fun_aggregate = 'mean'
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'), fun_aggregate = 'mean',
anova_table = list(es = "pes")
)
data <- read_csv(file="BasicRawSummary.csv")
data$posture = as.factor(data$posture)
data$posture = as.factor(data$posture)
data$cursor_type = as.factor(data$cursor_type)
data$wide = as.factor(data$wide)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
# ,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'), fun_aggregate = 'mean',
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time', fun_aggregate = 'mean',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time', fun_aggregate = 'mean',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
# ,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes",na.rm=TRUE)
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes"),na.rm=TRUE
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
, fun_aggregate = mean
)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
, fun_aggregate = mean,na.rm=TRUE
)
data$wide = as.factor(data$wide)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
, fun_aggregate = mean,na.rm=TRUE
)
data <- read_csv(file="BasicRawSummary.csv")
data$posture = as.factor(data$posture)
data$cursor_type = as.factor(data$cursor_type)
data$wide = as.factor(data$wide)
data = data[-which(is.na(data$target_in_count)), ]
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
a1 = aov_ez('subject_num','initial_contact_time',data,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
, fun_aggregate = mean,na.rm=TRUE
)
a1 = aov_ez('subject_num','initial_contact_time',data.mean(),within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','initial_contact_time')
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean(),within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
View(data.mean)
data <- read_csv(file="BasicRawSummary.csv")
data$posture = as.factor(data$posture)
data$cursor_type = as.factor(data$cursor_type)
data$wide = as.factor(data$wide)
data = data[-which(is.na(data$target_in_count)), ]
row.names(data) = NULL
data.mean <- aggregate(data$initial_contact_time,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide','initial_contact_time')
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
u
apa_print(a1, correction = "GG", es="pes")$dfull_result
apa_print(a1, correction = "GG", es="pes")$dfull_result
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
a1 = aov_ez('subject_num','initial_contact_time',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
summary(a1)$sphericity.tests
m1=emmeans(a1,~ cursor_type)
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
View(data.mean)
data.mean <- aggregate(data$mean_offset,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
data.aov <- with(data.mean,
aov(initial_contact_time ~ posture * cursor_type +
Error(subject_num / (posture * cursor_type)))
)
summary(data.aov)
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
c = 'mean_offset'
data.mean <- aggregate(data$c,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
data.mean <- aggregate(data$(c),
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
data.mean <- aggregate(data$[c],
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
data.mean <- aggregate(data$mean_offset,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type',c)
colnames(data.mean) = c('subject_num','posture','cursor_type','wide',c)
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
summary(a1)$sphericity.tests
apa_print(a1, correction = "none", es="pes")$full_result 													   # get latex and effect sizes with NO spherecity corrections
apa_print(a1, correction = "GG", es="pes")$dfull_result
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type','wide'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
print(aov)
print(a1)
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
print(a1)
colnames(data.mean) = c('subject_num','posture','cursor_type',c)
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
data.mean <- aggregate(data$mean_offset,
by = list(data$subject_num, data$posture,
data$cursor_type,data$wide),
FUN = 'mean'
,na.rm=TRUE
)
colnames(data.mean) = c('subject_num','posture','cursor_type',c)
a1 = aov_ez('subject_num','mean_offset',data.mean,within=c('posture','cursor_type'),
anova_table = list(es = "pes")
# , fun_aggregate = mean,na.rm=TRUE
)
